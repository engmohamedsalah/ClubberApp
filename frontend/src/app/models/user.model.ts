// Corresponds to the User entity/DTO from the backend
export interface User {
  id: string; // Or number, depending on your backend ID type
  username: string;
  email: string;
  // Add other relevant user properties if needed
}

